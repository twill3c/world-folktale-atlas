# SPEC.md — world-folktale-atlas(World Folktale Atlas AI)

<!-- scaffold template v1.30.0 から展開(2026-09-08)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

世界の民話・昔話のうち、**権利状態を一件ずつ確かめられたもの**だけを集め、
多言語 Embedding を通して「意味」の側から地理を眺めるアトラスを作る。

中心にある問いは一つである。

> **機械が測る「物語の似かた」は、言語や本ではなく、物語そのものを見ているのか。**

本プロジェクトはこの問いに「はい」と答えることを目的にしない。
**測って、答えが何であれ画面に出す**ことを目的とする。

---

## 2. この SPEC が V1.0 設計書から変更した点(実測にもとづく)

出発点は `World_Folktale_Atlas_AI_V1.0_完全実装設計書.md`(2026-09-08)である。
L0 の実測で前提が崩れた/足りなかった箇所を以下のとおり改める。**変更理由はすべて実測である。**

### 2.1 目玉の主張を設計書に追加する(設計書 §1.2 → 本 SPEC §3)★最重要

設計書は「地理的に離れた物語間の意味的類似を発見できる」を価値として掲げる(§1.2)が、
**その前提が成り立つかを測る手続きを持たない。**

Embedding が物語ではなく **言語** や **本(翻訳者・編者の文体)** を見ているだけでも、
類似度は同じように出る。UI は同じように動く。テストも緑になる。
**この構成のままでは、目玉が壊れていても誰も気づけない。**

よって本 SPEC は、アプリの主張を成立させる前提そのものを**落ちうるゲート**として登録する(§3・§9)。

### 2.2 「地域が似る」の主張には本内効果を先に差し引く(設計書 §35/§38 → 本 SPEC §3 H-03)

設計書は類似ネットワークと地図上の類似リンクを主要機能に置く(§35 レイヤー・§38)。
しかし本コーパスは**一冊が一地域に対応する**構造を持つ(『Folk-Tales of Bengal』= ベンガル、
『Philippine Folk Tales』= フィリピン)。したがって

> 同じ本の話どうしが似る ⇒ 同じ地域の話どうしが似る

が**構成上の必然として**成立してしまう。翻訳者・編者の文体・語彙・組版は本単位で共通だからである。
これを「地域の発見」と呼べば循環する(HC-045 と同型)。

よって **地理に関する主張は、本をまたぐペアだけで行う**(§9 G-04)。
本内ペアは対照として画面に併置する。

### 2.3 データ源の優先順位(設計書 §19 → 本 SPEC §5)

設計書は Gutenberg / Wikisource / Internet Archive / Wikimedia Commons の 4 源を挙げる。
L0 実測(2026-09-08)の結果、V1.0 は **Project Gutenberg 単源**とする。

| 源 | 実測結果 | V1.0 採否 |
|---|---|---|
| Project Gutenberg | `robots.txt` の Disallow は `/ebooks/search` のみ。本文取得 HTTP 200 | **採用** |
| gutendex.com(PG カタログ API) | HTTP 200。`count` と `formats` を返す | **採用**(発見と format URL に使う) |
| Wikisource API | HTTP 200 | 見送り(V1.1) |
| Internet Archive | 未検証 | 見送り(設計書 §19.3 自身が「存在すること自体は根拠にならない」と書く) |
| Wikimedia Commons | 未検証 | 見送り(V1.0 は画像を持たない) |

単源にするのは**取得できないから**ではなく、**権利確認の手続きを一種類に絞ると、
その一種類を最後まで正しくやりきれる**からである。源が増えた分だけ確認の穴が増える。

### 2.4 テキスト取得 URL は推測しない(実測)

`https://www.gutenberg.org/cache/epub/{id}/pg{id}.txt` は**全書に存在しない**。
実測: 2591/29021/38488/12814 は HTTP 200、**20050(独)/20972(仏)は HTTP 404**(6,398 バイトのエラーページ)。
よって取得 URL は**カタログの `formats` から取る**。推測した URL は使わない。
404 の本文(エラーページ)は 6KB あり、**サイズだけを見ると成功と区別できない**。
取得は HTTP ステータスと本文中の PG マーカーの両方で検証する(§7 T-DATA-01)。

### 2.5 分割は「本ごとの明示規則 + 件数オラクル」で行う(設計書 §26 → 本 SPEC §6)

設計書の ETL(§26)は Segmentation を一段として書くだけで、**民話集を一話ずつに割る**という
本プロジェクト固有の難所を扱っていない。単位は本ではなく話である。

L0 実測で、目次の形式は本ごとに異なることが分かった。

| 本 | 目次 | 備考 |
|---|---|---|
| PG 2591 Grimms' Fairy Tales | `CONTENTS:` + 字下げ列挙 | 下位項目(`1. HOW THEY WENT…`)と重複行(`the juniper-tree.`)が混ざる |
| PG 29021 Perrault | `CONTENTS.` + ページ番号 | 直後に `LIST OF ILLUSTRATIONS` が続き、取り違えると 20 件超になる |
| PG 38488 Folk-Tales of Bengal | 冒頭に目次なし | 前付けが長い |
| PG 12814 Philippine Folk Tales | 目次なし | 見出しのみで区切られる |

したがって**汎用の目次パーサは書かない**。本ごとに分割規則と期待件数を
`data/metadata/books.json` に明示し、ETL は期待件数との一致を表明する(HC-012)。
オラクルが見つからない本は `count_oracle: "none"` と書き、`review_required` を外さない。

### 2.6 ローカル実行環境の制約(実測)

- **Python から `huggingface.co` / `download.pytorch.org` の名前解決ができない**
  (`socket.gethostbyname` が `[Errno 11001]`)。`pypi.org` / `files.pythonhosted.org` は解決する。
  `curl` は両方 HTTP 200 を返す。**同じ機械で経路によって到達性が違う。**
- よって: 依存は PyPI から入れ、**モデルの重みは `curl` で取得してローカルから読む**。
  `transformers` / `sentence-transformers` の自動ダウンロードには依存しない(`local_files_only`)。
- 推論は `onnxruntime`(CPU)で行う。設計書 §5.2 の PyTorch 前提を本機の実測に合わせて改める。

---

## 3. 目玉の主張(先に登録する)

**外れたら外れたと画面に書く。** 通らなかったゲートは §9 に結果を残す。

| ID | 主張 | 落ちる条件 | 落ちたときにすること |
|---|---|---|---|
| H-01 | 多言語 Embedding は**言語ではなく物語**を見ている | 交差言語 P@1 が事前登録の閾値未満 | 「類似は言語を見ている」と画面に明記し、地図の類似リンクを撤去する |
| H-02 | 意味的類似は**本(翻訳者の文体)より物語**に効いている | 本内ペアと本間ペアの類似度分布が分離し、同一物語ペアが埋もれる | 類似度から本内効果を差し引いた指標に切り替える |
| H-03 | 地理的に近い文化圏の民話は意味的にも近い | 本をまたぐペアで、地理距離と意味距離の相関が有意でない | **不成立として画面に出す**(この主張は落ちる可能性が高いと事前に見ている) |

### H-01 の測り方(非循環)

同一の物語の**別言語版**を対にする。対応づけの根拠は**その本自身の目次の並び**であって、
モデルの出力ではない。したがってこのオラクルは循環しない。

- 指標: 交差言語 Precision@1(相手言語の全話 N 件のうち、正しい相手が第 1 位になる率)
- 帰無: ランダム対応 = 1/N
- 閾値: **P@1 ≥ 0.50**(事前登録。N ≈ 50 なら帰無 0.02)
- 追加の対照: 言語内平均類似度 vs 言語間平均類似度(言語で固まっていれば後者が系統的に低い)

### H-04 出来事・モチーフの NLI 推定(L-DL1 で登録。**モデルを走らせる前に書いた**)

| ID | 主張 | 落ちる条件 | 落ちたときにすること |
|---|---|---|---|
| H-04a | 多言語 NLI の零ショット推定は、既存の方式(e5 の説明文との近さ・引き金語の数)より正解集に当たる | 正解集で macro-AUC の差(NLI − 既存)の平均が 0.05 未満、または話で再標本化した 95% 区間の下端が 0 以下 | 話の画面の推定を差し替えない。結果は `/gates/` に出す |
| H-04b | NLI の付与は、言語が違っても同じ話で揃う | 独英グリム 26 組で、対の付与の一致率が、対でない組の一致率を順列検定 p < 0.01 で上回らない | 同上 |

**固定したもの(正解を付ける前・モデルを走らせる前):**

- ラベル 21 個と仮説文・判定の定義: `ml/nli_labels.py`
- モデル: `MoritzLaurer/multilingual-MiniLMv2-L6-mnli-xnli`(MIT)の fp32 ONNX。
  **選んだ根拠は精度ではない。** 明らかな含意の 3 例で含意確率が正しく立つか(mDeBERTa-v3-base の量子化版は
  「カエルが王子に変えられた」→「誰かが変身する」に 0.13 で不合格)と速度(MiniLM 10.2 組/秒、mDeBERTa 1.5 組/秒、
  4 スレッド、24 組、2026-09-17 実測)だけで決めた。int8 に動的量子化すると 19.8 組/秒になるが、
  含意確率が fp32 から平均 0.128・最大 0.454 ずれた(64 組、同日実測)ので採らない
- 話のスコア: 語 300 語・重なりなしのチャンクごとに 3 値 softmax の含意確率を出し、**話の中の最大値**。二値化は 0.5
- 正解集: `ml/nli_gold.py` の乱数で選んだ 45 話(各本 1 話 + 15 話)。**2 人の読み手が互いの答えを見ずに**
  `ml/nli_labels.py` の定義で付け、**二人が一致した項目だけを正解に使う**。一致率(Cohen κ)を併記する。
  読み手は Claude のサブエージェントであって、民話学の専門家ではない
- 主指標: 正解集で「ある」「ない」がそれぞれ 3 話以上あるラベルだけの macro-AUC。
  区間は話を単位に 2,000 回の再標本化(種 20260917)
- 比べる相手: ラベルごとに `ml/nli_labels.py` の `baseline`(e5 の z 値、または引き金語の出現率)
- 陽性対照: 正解で「ない」の話に、そのラベルが起きたと書いた一文を本文の中央に差し込むと、話のスコアが 0.5 以上になる割合 ≥ 0.80。
  **これが落ちたら H-04a の判定をしない**(仕掛けが効いていない)
- 陰性対照: NLI のスコア行列のラベル列を入れ替えると macro-AUC が 0.40〜0.60 に入る
- 長さの交絡(判定には使わず測って出す): 正解で「ない」項目の誤付与率を、話の語数の三分位の上下で比べる。
  「話の中の最大値」は長い話ほど高く出る方向に偏るはずなので、2 倍を超えたら画面に書く

**結果(2026-09-17、1,113 話・チャンク 5,809 個):**

- 正解集: 945 項目中 905 項目で二人が一致(κ = 0.91)
- 陽性対照 0.633 で基準未達 → **H-04a は判定しない**。参考の macro-AUC は NLI 0.586 / 既存 0.686、
  差 −0.100(95% 区間 −0.199〜−0.009)。区間はまるごと 0 の下にある
- **H-04b 不成立**(p = 0.992)。独語グリム 62 話がすべて 21 ラベル付与で、対の一致率が組み合わせによらず英語側の付与率(0.668)に一致した
- 長さの交絡: 誤付与率 短い三分の一 0.390 / 長い三分の一 0.921(2.36 倍)
- 事後の診断(判定に使わない): 陽性対照の一文**だけ**を前提にしても 21 ラベル中 13 で含意確率 0.5 未満
  (モデルが言い換えを読まない)。チャンクの含意確率 ≥ 0.5 の割合は 英語 0.280 / 独語 0.884
- 話の画面の推定は差し替えていない。結果は `/gates/` に出した

### H-05 筋の形(L-DL2 で登録。**窓の Embedding を計算する前に書いた**)

| ID | 主張 | 落ちる条件 | 落ちたときにすること |
|---|---|---|---|
| H-05a | 話の中で意味がどう動くか(筋の形)だけで、言語が違っても同じ話を探し当てられる | 独英グリムの対で、形だけの交差言語 P@1 が 0.20 未満、または順列検定 p ≥ 0.01 | 話の画面に「筋の形が似ている話」を出さない。結果は `/gates/` に出す |
| H-05b | 形は、話全体の Embedding(いまの近傍)に無い情報を足す | 英語全話の中から探す MRR で、「全体+形」−「全体だけ」の対で再標本化した 95% 区間の下端が 0 以下 | 近傍の並びは全体の Embedding のままにする |
| H-05c | 形は本(訳者の文体)の効果を受けにくい | 本内の組と本間の組の類似度の Cohen d が、形では全体の Embedding の d 以上 | 画面に書く |

**固定したもの(計算の前):**

- 窓: 語 150 語・重なりなし。末尾が 75 語未満なら直前の窓につなぐ。e5-small(`ml/embed.py` と同じモデル・接頭辞)
- 形: 窓ベクトルから話の平均を引き(文体・話題のように話の中で一定の成分を落とす)、
  話の中の相対位置 0〜1 で **8 点**に線形補間し、各点を L2 正規化する。
  二話の形の類似度 = 8 点それぞれのコサインの平均
- 窓が 4 つ未満(約 600 語未満)の話は形を持たない(比べない)
- 全体+形: 問い合わせごとに二つの類似度をそれぞれ z 化して足す(重みは 1:1、動かさない)
- 陽性対照: 同じ話を 75 語ずらして切り直した形で、自分自身を P@1 ≥ 0.90 で探し当てる(独語版グリム)。
  **これが落ちたら H-05a/b の判定をしない**(形が切り方で変わるなら、何を測っているか言えない)
- 陰性対照: 窓の順番を話ごとにでたらめに並べ替えると、形だけの P@1 が 0.10 以下に落ちる
  (中身は同じで順番だけ壊したもの。落ちなければ「形」は順番でなく中身を見ている)

**結果(2026-09-18、窓 10,519 個・形を持つ話 822 / 1,113):**

- 陽性対照 P@1 1.000(54 話)、陰性対照 P@1 0.040 → 判定する
- **H-05a 成立**: 形だけで独英グリムの相手を、英語版グリム 58 話の中から 100%、英語全 768 話の中からも 100%
  (順列検定 p = 0.0001)。同じ相手の中で、話全体の Embedding は 72.0%
- **H-05b 成立**: MRR 0.606(全体だけ)→ 1.000(全体+窓)、差 0.394(95% 区間 0.237〜0.559)
- **H-05c 成立**: 本内と本間の Cohen d は 1.144(全体)→ 0.337(窓・平均を引いた形)。小さくなるが消えない
- **ただし「順番が効いている」とは言えない(結果を見てから足した対照)**。位置を合わせず窓どうしを
  互いの最も近い相手で突き合わせる(順番を一切使わない)やり方でも、英語 768 話から P@1 1.000。
  事前登録の陰性対照は「位置を合わせて比べる限り」の話しか言えなかった。
  効いているのは**話を刻んで細かく比べていること**である。画面にもそう書いた

### H-06 ブラウザ内の意味検索(L-DL3 で登録。**作る前に書いた**)

| ID | 主張 | 落ちる条件 | 落ちたときにすること |
|---|---|---|---|
| H-06a | ブラウザで出す順位は、手元の fp32 で出す順位と同じものと見なせる | 20 の問いで、上位 10 件の重なりの平均が 0.80 未満、または 1 位の一致が 0.80 未満 | 「意味で探す」を画面に出さない。結果は `/gates/` に出す |
| H-06b | 日本語の問いから、その内容の話に届く | 和訳のある話の冒頭(日本語)を問いにして、その話自身が 1 位になる率が 0.50 未満 | 同上 |

**固定したもの(作る前):**

- 配るもの: 話のベクトル 1,113 × 384 を **int8 に量子化**して `public/data/vectors.bin`(約 0.43 MB)。
  尺度は全体で 1 つ(最大絶対値)。**本文もモデルの重みも配らない**(N-02 を改訂する)
- 問い側: `Xenova/multilingual-e5-small` の量子化 ONNX を **huggingface.co から実行時に読む**
  (118 MB。リポジトリには置かない。GitHub の 1 ファイル 100 MB を超えるため)。接頭辞は `query: `(コーパスと同じ)
- 問い 20 件は**日本語で人が書いたもの**(`ml/query_set.json`)。正解は持たない ——
  H-06a は**同じ問いに対する二つの経路の順位の一致**だけを見る(正解を要らない形にする)
- H-06b の問いは和訳の冒頭。**AI が作った訳なので、これは検索の質ではなく経路が通っていることの検査**である
- 対照: 問いを無関係な文(「今日の天気は晴れです」)にしたとき、上位が話題ごとに散らばること(順位の一致の検査が
  問いの内容に依らず通ってしまわないことを見る)

**ゲート:** G-17(二実装照合)/ G-18(順位の保存 = H-06a)/ G-19(交差言語の経路 = H-06b)/ G-20(問いの言語の交絡・結果を見てから足した)

**結果(2026-09-18、Chromium・q8・外部から 5.3 MB を 9 秒で読み込み、1 問 77 ミリ秒):**

- G-17 ブラウザの問いベクトルは手元の fp32 とコサイン平均 0.9967(最小 0.9956)
- **G-18 成立**: 上位 10 の重なり 0.885、1 位一致 0.800(最小の重なりは 0.6)
- **G-19 不成立**: 和訳の冒頭を問いにして自分の話が 1 位になったのは 0.267(上位 10 で 0.433)。偶然の 0.0009 よりはるかに高いが、登録した帯 0.50 に届かない。**帯は動かさない**
- **G-20 問いの言語が文化圏を引き寄せる**: 同じ意味の問いで 1 位が日本の話になったのは日本語 16/20・英語 1/20。コーパスの日本の話は 22 話(2.0%)しかない。日本語で問うと日本の話ばかり返る
- よって**「意味で探す」は画面に出さない**(`show_on_site: false`)。作った部品と測定結果は残し、`/gates/` に出す

---

## 4. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 権利確認済みの民話を 100 話以上収録し、全話に出典 URL・権利状態・確認日を持つ | must |
| F-02 | 世界地図上に収録話の文化圏を表示し、地域・言語・年代・本で絞り込める | must |
| F-03 | 全文・題名・地域・言語に対するキーワード検索 | must |
| F-04 | 話の詳細画面(本文全文・出典・権利・AI 分析) | must |
| F-05 | 多言語 Embedding による類似話検索(Top-K・スコア併記) | must |
| F-06 | UMAP による意味空間の散布図(色分け切替) | must |
| F-07 | クラスタリング(HDBSCAN 系)とクラスタ一覧 | must |
| F-08 | モチーフ・テーマの抽出と件数の地域比較 | must |
| F-09 | 物語構造(イベント列)と緊張曲線 | should |
| F-10 | 2〜5 話の比較画面 | should |
| F-11 | 登場人物ネットワーク | should |
| F-12 | 目玉の主張(§3)の測定結果を画面に出す(落ちた主張も) | **must** |
| F-13 | バイアス・ダッシュボード(言語・年代・源・地域の偏り) | should |
| F-14 | AI 推定と原資料の視覚的区別、および限界の明示 | must |

## 5. データ源と権利

### 5.1 採録の条件(全件を満たすこと)

1. Project Gutenberg に収録され、`formats` が示す本文 URL から取得できる
2. PG の書誌に基づき **米国パブリックドメイン**として提供されている
3. 出典 URL(書誌ページ)が恒久的に記録できる
4. 取得時に PG のヘッダ・フッタを除去し、**PG の商標・ライセンス文言を再配布しない**

### 5.2 権利フィールド(設計書 §20.3 に準拠)

`license_status` / `license_name` / `jurisdiction` / `verification_date` /
`rights_evidence_url` / `redistribution_allowed` / `commercial_use` / `derivative_use` /
`ml_processing_status` / `notes`

### 5.3 権利レイヤーの分離(設計書 §21)

原作 / 翻訳 / 版 / 註 / 挿絵 / スキャン / 翻刻 を別々に記録する。
本コーパスは**19〜20 世紀の翻訳・編集**を通して手に入るため、
「原話は古い」ことと「この版が PD である」ことを混同しない。

## 6. ETL と分割

```
gutendex カタログ
   → 本の選定(data/metadata/books.json に明示)
   → formats から本文 URL を取得(推測しない)
   → 取得(HTTP 200 + PG マーカーの両方で検証)
   → PG ヘッダ・フッタ除去
   → 本ごとの規則で一話ずつに分割
   → 件数オラクルとの一致を表明
   → 正規化・言語判定・重複検出
   → Embedding → 近傍・クラスタ・UMAP
   → 静的 JSON 生成
```

分割規則は本ごとに `books.json` の `split` に書く。規則は以下のいずれか。

| 規則 | 内容 | 件数オラクル |
|---|---|---|
| `toc` | 目次に列挙された題名を本文中の単独行として探す | 目次の項目数 |
| `heading` | 明示した正規表現に一致する行を境界にする | 前書き等の記載、または `none` |
| `manual` | 明示した境界行を列挙する | 列挙数 |

本ごとの追加設定(既定は無効。**当てはまる本にだけ明示する**):

| 設定 | 内容 | なぜ既定にしないか |
|---|---|---|
| `skip` | 話でない目次項目(前付け・区分見出し)を落とす | 本ごとに違う |
| `alias` | 目次の表記と本文の見出しの対応を明示する | 綴りの揺れは本ごとの事実 |
| `toc_flow` | 目次が流し組みの本で、ブロック全体を一続きにしてローマ数字の前で割る | 副題にローマ数字を含む題名が割れる本がある |
| `toc_ordered` | 目次が本文の並びどおりの本で、一致位置の単調増加を課す | 目次がアルファベット順の本がある(独語版グリム) |

### 6.1 場所を持てない本

伝承が単一の土地に対応しない本がある(ユダヤのディアスポラ)。
その本は緯度経度を持たず、`location_precision: "unknown"` とし、**地図に印を打たない**。
もっともらしい座標を作って地図に載せることはしない(SPEC §24)。

## 7. データ品質検査(§9 のゲートとは別に、全件で走る)

| ID | 検査 |
|---|---|
| T-DATA-01 | 取得本文に PG の START/END マーカーが両方ある(404 ページを本文と誤認しない) |
| T-DATA-02 | 全話に `story_id` / `title` / `text` / `language` / `source_url` / `license_status` / `verification_date` がある |
| T-DATA-03 | `story_id` に重複がない |
| T-DATA-04 | 本文が空でない・最短語数を下回らない |
| T-DATA-05 | PG のヘッダ・フッタ・商標文言が本文に残っていない |
| T-DATA-06 | 分割件数が `count_oracle` と一致する(`none` の本は `review_required` が立つ) |
| T-DATA-07 | 日本語本文へのキリル文字混入検査(`harness/text_hygiene.py`) |
| T-DATA-08 | 座標を持つ話は、その `location_precision` が `exact`/`city`/`region` のいずれかである |

## 8. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | Vercel Hobby 無料枠で運用でき、cron・DB・サーバ推論を使わない | 静的出力のみで `next build` が通る |
| N-02 | ブラウザへ配るのは**量子化した話のベクトル 1 本だけ**(`vectors.bin`、int8、約 0.43 MB)。生の float ベクトル・モデルの重み・索引ファイルは配らない(L-DL3 で改訂。**改訂前は「Embedding 本体を送らない」**で、意味検索を作るために緩めた) | `public/data` に `.npy`/`.npz`/`.faiss` と、`vectors.bin` 以外の `.bin` を置かない検査。`vectors.bin` は大きさの上限と、量子化の尺度を書いた meta の存在を検査する |
| N-03 | 初期表示は話の本文を含まない(オンデマンド取得) | 初期 JSON のサイズ上限 |
| N-04 | キーボード操作・コントラスト・色以外の手がかり・reduced-motion | UI テスト |
| N-05 | フリート共通フッタ規約(5 項目・下部固定)に従う | 目視 + テスト |

## 9. ゲート(通らなければ出荷しない/落ちたら画面に書く)

| ID | ゲート | 閾値 | 結果 |
|---|---|---|---|
| G-01 | 収録話数 | ≥ 100 | **✓ 1,113 話 / 30 冊 / 29 文化圏**(L9 でジャマイカを追加。L8 は 977 話 / 29 冊、L7 までは 709 話 / 23 冊 / 22 文化圏) |
| G-02 | 出典 URL・権利状態・確認日の充足率 | 100% | **✓ 100%** |
| G-03 | 分割件数が件数オラクルと一致した本の割合 | 100%(`none` を除く) | **✓ 30/30 冊**(目次照合 24 冊 + 通し番号照合 6 冊) |
| G-04 | 地理の主張は本をまたぐペアだけで行っている | 実装で保証 | **✓**(`ml/evaluate.py` / 画面の札) |
| G-05 | **H-01 交差言語 P@1** | ≥ 0.50 | **✓ 0.731**(英語版グリム 64 話の中から。英語全 647 話でも 0.462) |
| G-06 | Embedding の二実装照合(onnxruntime と独立実装) | 最大絶対差 < 1e-4 | **✓ 1.04e-6** |
| G-07 | 本内ペアと本間ペアの類似度分布を測って画面に出した | 実施 | **✓ Cohen d = 0.72**(L9、30 冊。L8 まで 0.77) |
| G-08 | AI 推定と原資料が視覚的に区別されている | 目視 + テスト | **✓**(実線/破線 + 札。色に依存しない) |
| G-09 | 落ちたゲートが画面に書かれている | 実施 | **✓**(`/gates/` に H-03 の判定の履歴を掲載。21 冊では「判定できない」、L8 の 27 冊で帯を動かさずに「相関がある」。交絡と多重性の注記つき) |
| G-10 | 分類器の付与分布を群ごとに数えた(HC-227) | 無付与率の群間差 ≤ 0.40 / 最頻ラベル ≤ 0.25 | **✓**(`data/analysis/label_distribution.json`) |
| G-11 | H-04 の陽性対照(一文の差し込みで立つ) | ≥ 0.80 | **✗ 0.633**(60 組、2026-09-17)。よって H-04a は判定しない |
| G-12 | H-04 の陰性対照(ラベルの列の入れ替えで偶然の帯へ) | macro-AUC 0.40〜0.60 | **✓ 0.579** |
| G-13 | NLI の付与分布(HC-227)。**壊れていたら話の画面へ出さない** | 言語ごとの無付与率 ≤ 0.40 / 最頻ラベル ≤ 0.25 / 全ラベル付与率 ≤ 0.25 / 言語間の平均付与数の差 ≤ 3.0(後ろ二つは結果を見てから足した) | **✗ 全ラベル付与 50.9%・平均付与数 英 13.93 / 独 20.66**。前の二つだけでは緑のまま通った。話の画面には出していない |
| G-14 | H-05 の陽性対照(75 語ずらして切り直しても自分を引き当てる) | P@1 ≥ 0.90 | **✓ 1.000**(独語版グリム 54 話、2026-09-18) |
| G-15 | H-05 の陰性対照(窓の順番を壊すと当たらなくなる) | P@1 ≤ 0.10 | **✓ 0.040** |
| G-16 | 「順番が効いている」と言えるか(結果を見てから足した対照) | 順番を使わない突き合わせより高いこと | **✗ どちらも 1.000**。順番の寄与は示せていないと画面に明記 |
| G-17 | ブラウザ(量子化)と手元(fp32)の問いベクトル | ずれを測って出す | **平均コサイン 0.9967 / 最小 0.9956**(20 問、2026-09-18) |
| G-18 | 順位の保存(H-06a) | 上位 10 の重なり ≥ 0.80 かつ 1 位一致 ≥ 0.80 | **✓ 0.885 / 0.800** |
| G-19 | 交差言語の経路(H-06b) | 和訳の冒頭で自分の話が 1 位になる率 ≥ 0.50 | **✗ 0.267**(上位 10 では 0.433、偶然は 0.0009)。登録どおり「意味で探す」は画面に出さない |
| G-20 | 問いの言語が文化圏を引き寄せるか(結果を見てから足した対照) | 測って出す | **✗ 引き寄せる**。同じ意味の問いで、1 位が日本の話になったのは日本語 16/20・英語 1/20(コーパスの日本は 2.0%) |

## 10. スコープ外(設計書 §2.2 を継承)

- 民話の歴史的起源・伝播経路の断定
- 民族・文化・宗教の優劣の評価
- 巨大 LLM の独自学習、Vercel 上での重量級推論
- AI 推定 ATU 分類を正式な学術分類として扱うこと
- ユーザー投稿・アカウント・モデレーション
- 音声(FolkSound Atlas との統合は将来)
